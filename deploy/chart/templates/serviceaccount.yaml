{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "databricks.serviceAccountName" . }}
  labels:
    {{- include "databricks.labels" . | nindent 4 }}
{{- end }}
