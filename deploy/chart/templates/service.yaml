apiVersion: v1
kind: Service
metadata:
  name: {{ include "databricks.fullname" . }}
  labels:
    {{- include "databricks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "databricks.selectorLabels" . | nindent 4 }}
